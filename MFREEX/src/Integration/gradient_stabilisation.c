#include "Integration/gradient_stabilisation.h"

int gradient_stabilisation(SCNI_OBJ * scni_,  shape_function_container * sf_point, meshfreeDomain * mfree )
{

	// VEC * di = mfree->di;
	// int num_points = mfree->num_nodes;
	// int dim = mfree->dim;
	// int num_cells = scni_->num_points;





	// MAT * phi_der;
	// IVEC * neighbours;
	// MAT * phi_der_at_L;
	// IVEC * neighbours_L;
	// MAT * phi_der_der ;


	// for ( int i = 0 ; i < num_cells ; i++)
	// {


	// 	neighbours = sf_point->sf_list[i]->neighbours;
	// 	phi_der_der = sf_point->sf_list[i]->d2phi;



	// 	scni_->scni[i]->Bs = m_get(5,dim*neighbours->max_dim);



		







	// }



	return 0;
}
